export default class Triangle {
  // isEquilateral: boolean = false;

  constructor() { }

  // constructor(private a: number, private b: number, private c: number) {
  //   // this.isEquilateral = this.getIsEquilateral()
  // }

  // getIsEquilateral() {
  //   const [a, b, c] = this.sides

  //   return a === b && b === c
  // }
}